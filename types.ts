export interface NavItem {
    label: string;
    href: string;
}

export interface ClientLogo {
    name: string;
    isWide?: boolean;
}

export interface ServiceItem {
    title: string;
    description: string;
}
